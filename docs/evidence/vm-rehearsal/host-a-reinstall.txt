Playkeeper 0.1.0-dev+0c99fa7b6277 installer

Checking this server (nothing is changed yet):
  [ok  ] Administrator rights       Running as root.
  [ok  ] Operating system           Ubuntu 24.04 LTS (the tested platform).
  [ok  ] CPU architecture           x86_64 (amd64).
  [ok  ] Service manager            systemd is running.
  [ok  ] Memory                     2971 MB RAM.
  [ok  ] Disk space                 16.1 GB free under /var/lib.
  [ok  ] Port 8443                  Free for the web panel (HTTPS).
  [ok  ] Port 25565                 Free for the Minecraft players.
  [ok  ] Existing Minecraft setups  None found.
  [info] Previous Playkeeper data   Found worlds and backups from an earlier Playkeeper install in /var/lib/playkeeper; they will be reused, not changed.
  [info] Docker                     Docker is not installed. The installer will install Ubuntu's docker.io package.
  [info] Firewall                   No active ufw firewall. If your provider has a cloud firewall, allow these ports there: 8443/tcp and 25565/tcp.
  [info] HTTPS                      A self-signed certificate will be generated on this server. Your browser will ask you to trust it; compare the fingerprint the installer prints.

Playkeeper will make these changes:
  Packages:  install docker.io from Ubuntu's archive (with the packages it depends on)
  Users:     create 'playkeeper' (runs the web panel; no login shell; not in the docker group)
             create 'playkeeper-mc' (owns world files; the Minecraft container runs as this user)
  Files:     /usr/local/bin/playkeeper
             /etc/playkeeper/config.json
             /etc/systemd/system/playkeeper-agent.service and /etc/systemd/system/playkeeper-panel.service
  Data:      reuse /var/lib/playkeeper (existing worlds and backups are kept as they are)
  Services:  playkeeper-agent (root; local Unix socket only, no network port)
             playkeeper-panel (HTTPS on port 8443)
  Ports:     8443/tcp web panel now; 25565/tcp Minecraft once you create a server
  Untouched: your other services, existing Docker containers, SSH and firewall rules

To undo later: sudo playkeeper uninstall   (removes Playkeeper, keeps your worlds and backups)


Installing:
  • install Docker (docker.io)
  • create user playkeeper
  • create user playkeeper-mc
  • create directories
  • install /usr/local/bin/playkeeper
  • write /etc/playkeeper/config.json
  • generate HTTPS certificate and first-run setup code
  • install and start systemd services
  • write install manifest

Playkeeper is running.

  1. Open https://198.51.100.10:8443 and sign in with your existing admin account.
  2. Your browser will warn that the certificate is self-signed. Continue only if it shows
     this SHA-256 fingerprint:
       30:FE:C6:86:B4:46:F6:72:41:C6:72:55:1D:64:99:E9:54:09:9B:ED:5A:F8:74:C6:2A:DC:48:44:28:7A:D8:55
  3. Your worlds and backups were kept; the server starts again if it was running before.

If 198.51.100.10:8443 is not your public address, use your VPS's public IP instead.
Forgot the password? sudo playkeeper reset-password <username>
Uninstall any time: sudo playkeeper uninstall  (keeps your worlds and backups)
Install finished in 9s.
after reinstall, marker check: 118, 72, 47 has the following block data: ["pk-53425a3c0706", "", "", ""]
